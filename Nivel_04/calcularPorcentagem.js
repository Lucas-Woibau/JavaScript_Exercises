function Calcular(nominador = 0, denominador = 0){
    let resultado = nominador / denominador;
    console.log(nominador + " equivale a " + resultado*100 + "% de " + denominador);
}

Calcular(30, 60);
